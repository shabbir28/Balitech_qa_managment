# Testing & Ad-hoc Scripts Guideline

Whenever creating temporary, test, debug, check, dump, or scratch scripts:
- **Always** create them inside `backend/dev_scripts/`.
- Do **not** place test or scratch files directly in the project root or `backend/` root directory to maintain a clean workspace.
